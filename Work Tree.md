Goal: Create a succession of links that are related through the excalibrain ontology. Should have a tree like structure for each set of connections

Testing:
```dataviewjs
let testPage = dv.page("01Action plans")
dv.span(testPage)
```
